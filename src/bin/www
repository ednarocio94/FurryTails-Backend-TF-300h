// 3. Iniciar el servidor
import app from "../../app";

const port = process.env.PORT || 3000;
app.listen(port, ()=>{
    console.log(" holi, El servidor está ejecutándose correctamente, en el puerto ", port);
});
